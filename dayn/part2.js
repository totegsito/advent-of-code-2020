const inputCase = ``;

const testCase = ``;

const parseInput = (input) => {

}

const resolve = (input) => {

}

const testInput = parseInput(testCase);
const testSolution = resolve(testInput);

console.assert(testSolution === 'Whatever');

const input = parseInput(inputCase);
const solution = resolve(input);

console.table({ solution });
